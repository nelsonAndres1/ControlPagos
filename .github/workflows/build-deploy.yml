name: Build and Deploy

on: 
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: '17.x'
    - uses: actions/cache@v1
      with:
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    
    - name: NPM Install
      run: npm install
    - name: NPM Install Angular
      run: npm install -g @angular/cli > /dev/null
    - name: NPM build Angular Production
      run: ng build --prod
    - name: Delete Existing Files0
      run: sudo sshpass -p ${{ c0mf42022 }} -v ssh -o StrictHostKeyChecking=no ${{ comfa }}@${{ 192.168.6.34 }} 'rm -rf /home/comfa/ControlPagos/*'

    - name: Compress and Copy New Files to server
      run: |
        cd dist/
        tar -czvf ../../ControlPagos.tar.gz ./
        cd ..
        cd ..
        sudo sshpass -p ${{ c0mf42022 }} scp -v -o StrictHostKeyChecking=no -r app.tar.gz ${{ comfa }}@${{ 192.168.6.34 }}:/home/comfa/ControlPagos
    - name: Uncompress new Files
      run: sudo sshpass -p ${{ c0mf42022 }} -v ssh -o StrictHostKeyChecking=no $${{ comfa }}@${{ 192.168.6.34 }} 'cd /home/comfa/ControlPagos/ && tar -xzvf ControlPagos.tar.gz && rm ControlPagos.tar.gz'

        
